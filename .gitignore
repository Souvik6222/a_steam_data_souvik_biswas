# Dependencies
node_modules/

# Environment variables (never commit secrets)
.env

# Dataset files — can be very large (50 MB+)
src/data/*.json
a_steam_data_2021_2025.json

# OS / editor artifacts
.DS_Store
Thumbs.db
*.log
